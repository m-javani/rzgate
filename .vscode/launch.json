{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (Single Mode)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/rzproxy",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "RUST_LOG": "rzproxy=debug",
            },
            "preLaunchTask": "cargo build"
        }
    ]
}